package core.controller.signals.components {
import org.osflash.signals.Signal;

public class CollageItemSignal extends Signal {
    public function CollageItemSignal() {
        super(int);
    }
}
}
