package core.controller.signals.components {
import org.osflash.signals.Signal;

public class AddNewPhotoSignal extends Signal {
    public function AddNewPhotoSignal() {
        super(int);
    }
}
}
